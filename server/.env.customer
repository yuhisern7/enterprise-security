# STEP 1: Copy this file
# cp .env.example .env

# STEP 2: Edit ONLY these 3 lines with your relay server IP
RELAY_ENABLED=true
RELAY_URL=wss://YOUR_RELAY_SERVER_IP:60001
RELAY_API_URL=https://YOUR_RELAY_SERVER_IP:60002

# STEP 3: Set unique customer details
CUSTOMER_ID=customer-japan-001
PEER_NAME=japan-tokyo-office

# STEP 4: Start container
# docker compose up -d

# That's it! Your AI will now download pre-trained models from relay server.
# Check logs: docker compose logs | grep "Relay trained models using"

# ============================================================
# Optional: P2P Sync Between Your Own Containers
# ============================================================
# If you have multiple containers (office + branch + cloud),
# enable P2P to sync between YOUR containers only:

# P2P_SYNC_ENABLED=false
# PEER_URLS=https://office-ip:60001,https://branch-ip:60001

# ============================================================
# Optional: Advanced Settings
# ============================================================
# DASHBOARD_PORT=60000
# P2P_PORT=60001
# AI_LEARNING_ENABLED=true
# SIGNATURE_MODE=auto
