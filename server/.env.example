# AI Security System Configuration
# Copy this file to .env and configure your API keys

# VirusTotal API Key (REQUIRED - for IP reputation checks)
# Get your FREE key at: https://www.virustotal.com/gui/join-us
# Free tier: 4 requests/minute
VIRUSTOTAL_API_KEY=

# AbuseIPDB API Key (optional - for community IP blacklist)
# Get your free key at: https://www.abuseipdb.com/
ABUSEIPDB_API_KEY=

# Port Configuration
# Using high ports (60000+) to avoid conflicts with existing services
# You can change these if ports are already in use
DASHBOARD_PORT=60000  # Dashboard web interface (HTTP)
P2P_PORT=60001        # P2P mesh synchronization (HTTPS)

# P2P Mesh Network (optional - for peer-to-peer threat sharing)
# Every container is equal - no central server needed!
# When A gets attacked, B and C learn automatically
# Example (use P2P_PORT from each peer): https://192.168.1.100:60001,https://192.168.1.101:60001
PEER_URLS=
PEER_NAME=auto  # Give your container a name (e.g., office-1, home-main)
P2P_SYNC_ENABLED=false
P2P_SYNC_INTERVAL=180  # Sync every 3 minutes

# ExploitDB Signature Distribution (NEW!)
# Controls how this container handles ExploitDB attack signatures
# - auto: Auto-detect (has ExploitDB = master, no ExploitDB = client) [RECOMMENDED]
# - master: Force master mode (serves signatures to other containers)
# - client: Force client mode (receives signatures from peers via PEER_URLS)
# Windows/Mac: Use "client" to skip 500MB ExploitDB download!
SIGNATURE_MODE=auto

# ExploitDB Configuration
# Path to local ExploitDB database (auto-downloaded during setup)
EXPLOITDB_PATH=/app/AI/exploitdb

# Timezone Configuration
# You can change this in System Settings dashboard
# Examples: Asia/Kuala_Lumpur, Asia/Singapore, Asia/Tokyo, Europe/London, US/Eastern, UTC
TZ=Asia/Kuala_Lumpur

# Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=0

# Security Settings
MAX_BLOCKED_IPS=10000
THREAT_LOG_MAX_SIZE=100000
AUTO_BLOCK_ENABLED=true

# Enterprise API Settings (for companies integrating your system)
ENABLE_ENTERPRISE_API=true
API_RATE_LIMIT=1000

# Learning Mode
AI_LEARNING_ENABLED=true
AUTO_TRAIN_THRESHOLD=5
EXPLOITDB_SCRAPER_ENABLED=true
